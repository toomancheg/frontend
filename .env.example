# Пусто или не задавайте — браузер ходит на тот же origin (/api/...), Next.js проксирует на backend (см. API_PROXY_TARGET).
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
# Куда проксировать /api в dev (порт как в корневом .env: API_PORT, по умолчанию 8000):
# API_PROXY_TARGET=http://127.0.0.1:8000

